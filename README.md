Uploaded a short bash script that reads from a file all the IPs to be blocked, creates a blacklist from it, and then apply the corresponding iptables rules blocking all the traffic from/to these IPs.
